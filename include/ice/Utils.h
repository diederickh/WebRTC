#ifndef ICE_UTILS_H
#define ICE_UTILS_H

#include <random>
#include <string>

namespace ice {

  std::string gen_random_string(const int len);        /* generates a random alpha-num string with the given len. */
 
} /* namespace ice */
#endif
